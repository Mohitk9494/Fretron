import { PartnerDetailDirective } from './partner-detail.directive';

describe('PartnerDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new PartnerDetailDirective();
    expect(directive).toBeTruthy();
  });
});
